# The Butterfly Insights

The Butterfly Insights website.

## Setup

First, run the development server:

```bash
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## Format the code

To format the code, run:

```bash
bun format
```
